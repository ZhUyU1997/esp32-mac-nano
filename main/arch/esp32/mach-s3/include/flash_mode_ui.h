#ifndef MACH_S3_FLASH_MODE_UI_H
#define MACH_S3_FLASH_MODE_UI_H

/* Full-screen "Flash Mode" page shown during the whole flash-mode boot.
 * Exits: flashing from a browser (auto-reboots), or power cycle. */
void flash_mode_ui_show(void);

#endif
